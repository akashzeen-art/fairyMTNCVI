server {
    listen 80;
    listen [::]:80;
    server_name mfairytales.com www.mfairytales.com;

    root /var/www/vasnumero/fairytales_MTN_CLI/dist;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
    gzip_min_length 256;

    # VAS API proxy (avoids CORS — only for this site)
    location /adpoke/ {
        proxy_pass http://68.183.88.91;
        proxy_http_version 1.1;
        proxy_set_header Host 68.183.88.91;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA — react-router routes (/account, etc.)
    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    access_log /var/log/nginx/mfairytales.com.access.log;
    error_log  /var/log/nginx/mfairytales.com.error.log;
}
